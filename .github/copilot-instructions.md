# Project instructions

- This is a Vite Vanilla JavaScript site.
- Use `npm run dev` for local development.
- Run `npm run build` before publishing.
- Publish the `dist` directory with `npm run deploy`.
- Keep the user site compatible with the repository root URL `https://VishnuKeerthan.github.io`.
